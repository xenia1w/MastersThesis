"""Baseline-vs-prompted comparison tools for the earnings21 full-call runs.

Two CLIs share the helpers here:
  * ``compare_call``     — one call vs baseline, with a word-level fixed/degraded diff.
  * ``compare_approach`` — a whole approach vs baseline: per-call table + averages.
"""
