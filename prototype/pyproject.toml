[project]
name = "prosody-prototype"
version = "0.1.0"
description = "Simple prosodic feature extraction prototype."
requires-python = ">=3.10"
dependencies = [
  "librosa>=0.10.1",
  "numpy>=1.24",
  "soundfile>=0.12",
]

[tool.uv]
package = false
